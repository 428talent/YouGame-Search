export const applicationConfig = {
  port:8080,
  searchGameIndex:'game-index',
  elasticHost:'localhost:9200',
  elasticLog:'trace',
};

export const logService = {
  host:'http://localhost:5002',
  applicationId:'search',
  instance:'main',
};
